# 피드백·학습 흐름 개선 진행 기록

기준 문서: [2026-09-11 검토](feedback-learning-review-2026-09-11.md).
사용자 요청: 항목을 하나씩 구현하고, 사용 한도를 고려하여 1시간 간격으로 재개하며, 검증한 변경을 `KSH0660/Yumi-Opic`에 푸시한다.

## 재개 지점

- 상태: **사용 한도 회복 대기 — 구현 시작 전**.
- 다음 항목: **P1-01**. 아래 순서대로 진행하며, 이미 진행 중인 항목이 있으면 그것부터 끝낸다.
- 작업 디렉터리: `/Users/ksunho0660/Documents/Yumi-Opic`.
- 작업 브랜치: `codex/feedback-learning-loop`.
- 원격: `origin` = `https://github.com/KSH0660/Yumi-Opic`.
- 초기 기준: 기존 작업 `4b37dd9`와 `origin/main`의 `77d4efc`를 병합해 보존했다. 제품 코드 차이는 없었다.
- 자동화: `yumi-opic` / **Yumi-Opic 피드백 개선 이어가기**. 이 Codex 작업을 매 1시간마다 깨우는 heartbeat로 생성되었으며 상태는 `ACTIVE`이다.
- 현재 구현 변경: 없음. 기준 문서와 이 진행 기록만 준비했다.
- 처음 확인한 사용량: 2026-09-11 21:29 KST, 5시간 창 87%, 주간 창 29% 사용. 당시 5시간 창 초기화 예정은 **2026-09-12 01:53:45 KST**였다. 다음 실행에서는 반드시 최신 값을 확인한다.
- 미커밋 구현 / 실패한 검사 / 미푸시 커밋: 초기 설정 완료 시 아래 실행 기록 및 Git 상태로 확인한다.

## 매 실행의 규칙

1. 먼저 사용량 도구로 계정의 현재 한도를 확인한다. `rateLimitsByLimitId`가 있으면 그것을 우선하고 사용 중인 모델에 적용되는 모든 창을 살핀다. 5시간 등 단기 창이 85% 이상이거나 주간 창이 90% 이상이면 새 구현을 시작하지 않는다. 사용량 조회 불가·한도 도달·지출 제한도 작업 확대 없이 체크포인트 후 종료한다. 다음 정기 실행에서 다시 확인한다. 사용량이 회복될 때까지 장시간 sleep, 바쁜 재시도, 즉시 반복 실행을 하지 않는다. 리셋 크레딧 사용·추가 구매·모델 변경은 자동으로 하지 않는다.
2. 여유가 있으면 이 파일의 재개 지점, 기준 문서의 해당 절, 적용되는 저장소 지침과 필요한 코드만 읽는다. 전체 조사를 매번 반복하지 않는다. 한 실행에 **미완료 항목 하나 또는 그 안의 작은 검증 가능한 단위 하나**만 진행하고 종료한다. 큰 항목은 먼저 아래 표에 세부 단위를 적어 여러 실행으로 나눈다.
3. 시작 전에 상태를 `진행 중`으로 기록하고, 범위·예정 수정 파일·완료 조건을 적는다. 약 25분의 작업 시점 또는 큰 변경 직후에 사용량을 다시 확인해 정리 시간을 남긴다. 실제 안전한 중단점까지 유연하게 정리하되, 여유가 줄면 새 하위 작업을 시작하지 않는다. 모델이 중단되더라도 작업 파일과 이 기록에서 복구할 수 있어야 한다.
4. 사용자 변경을 보존한다. `git status`, 현재 브랜치, remote URL을 먼저 확인하고, 기록되지 않은 수정은 임의로 스테이징·삭제·덮어쓰기하지 않는다. 다른 작업과 충돌하면 분리된 작업 공간에서 진행할 수 있는지 판단하고, 해결할 수 없는 충돌만 기록한다. 중복 실행·이미 진행 중인 동일 작업을 시작하지 않는다.
5. 푸시 전 `git fetch origin`으로 원격 상태를 확인한다. 작업 브랜치 원격 갱신이 있으면 이력을 보존하여 통합하고 관련 검증을 다시 한다. 강제 푸시, 원격 브랜치 삭제, 사용자 작업 초기화는 하지 않는다. `upstream`에는 푸시하지 않는다. `main` 병합·서비스 배포는 별도 요청이 없는 한 하지 않는다.
6. 동작 변경에는 의미 있는 회귀 검증을 포함한다. 저장소 표준은 `npm test`, `npm run typecheck`, `npm run build`이다. 빌드와 타입 검사는 생성 파일 경합을 피하도록 순차 실행한다. 문서만 바꾼 실행에는 `git diff --check`와 문서 검토면 충분하다. UI 변경은 해당 UI 스킬을 적용하고 가능한 로컬 브라우저에서 휴대폰/데스크톱 크기·키보드·레이아웃을 확인한다. 실제 모바일 마이크, 유료 API, 사람 평가를 수행하지 못했으면 그 한계를 그대로 남긴다.
7. 성공한 단위를 관련 파일과 이 진행 기록만 명시적으로 스테이징하여 커밋한다. `git push -u origin codex/feedback-learning-loop`로 푸시하고 원격 브랜치 SHA가 로컬 HEAD와 같은지 확인한다. 미완성·검증 실패 코드는 완료로 표시하거나 정상 변경으로 푸시하지 않는다. 중단된 변경은 로컬 파일과 아래 재개 메모로 보존한다. 푸시 실패 시 검증된 로컬 커밋을 보존하고 다음 실행에서 먼저 푸시를 재시도한다.
8. 종료 전 재개 지점·완료/진행 상태·검증·수정 파일·다음 행동·미푸시 여부를 갱신한다. 커밋 자체의 SHA는 그 커밋 안에 넣으려 하지 말고 다음 실행에서 기록하거나 `git log`로 확인한다. 기다리는 상태가 똑같으면 불필요한 문서 커밋과 알림을 만들지 않는다.
9. 사용자에게는 항목 완료와 푸시, 새로운 실패, 필요한 실제 기기/사용자 확인, 전체 완료 같은 의미 있는 변화만 알린다. 동일한 사용량 대기는 조용히 끝낸다.
10. 모든 구현과 검증·푸시가 끝나면 최종 결과를 기록하고 이 heartbeat를 도구로 일시정지한다. 사람 평가나 실제 기기가 없어서 독립 작업이 더 이상 불가능한 경우에도 그 항목을 `외부 검증 대기`로 남기고 한 번 알린 뒤 일시정지한다. 대기를 전체 완료로 바꾸지 않는다. 재개 요청을 받으면 같은 자동화를 갱신한다.

위 사용량 임계값과 실행 길이는 이 작업의 보수적인 운영 기준이다. 토큰을 정확히 강제 제한하는 장치는 아니며, 계정 한도나 앱 실행 상태로 정기 실행이 지연될 수 있다. 로컬 파일을 쓰는 자동 실행에는 컴퓨터와 Codex 앱이 실행 중이어야 한다.

## 구현 체크리스트

`대기 → 진행 중 → 완료`로 바꾼다. 수행하지 못한 현실 검증은 `외부 검증 대기`로 구분한다. 검토 문서의 모든 내용을 한 번에 바꾸지 않고 다음 의존 순서로 처리한다.

| ID | 상태 | 범위와 완료 조건 |
| --- | --- | --- |
| P1-01 | 대기 | **역할극 관련 경험 분리.** `roleplay_experience`를 전화 역할극과 구분하여 과거 경험·시간 관계·전개 기준으로 처리한다. 질문하기/문제 해결 역할극의 기존 동작을 보호한다. 생성 프롬프트와 유형별 회귀 검증을 확인한다. |
| P1-02 | 대기 | **버전 있는 피드백 데이터.** 새로운 질문 수행 기준·판단 상태·강점·목표·근거·분석 범위와 기준/모델 버전을 정의하고 검증·저장을 연결한다. 기존 `topic/detail/feeling` 기록은 당시 기준으로 계속 읽고 표시하며 새 평가로 조용히 변환하지 않는다. |
| P1-03 | 대기 | **질문별 피드백 생성.** 공통 두괄식/디테일/감정 의무와 고정 카테고리 우선순위를 없앤다. 질문 수행→의미 왜곡/반복 문제→다듬기 순으로 실제 근거에 맞게 목표 1개와 추가 0~2개를 제안한다. 문제 없음·판단 보류·해당 없음, 질문에 명시된 요구 수, 사실을 만들어내지 않는 힌트/예시를 처리한다. 음성 근거 없는 발음·유창성 판정을 보류한다. |
| P1-04 | 대기 | **첫 화면의 목표와 근거.** 강점 1개·핵심 목표·근거·연습/재답변 행동을 우선 배치한다. 9개 활동 통계·전체 답변·Before/After·추가 피드백은 상세로 옮기며, 고유 단어/문장/AI 수정 개수를 성취 지표에서 제거한다. 목표 선택을 지원하고 리포트·과거 기록을 유지한다. |
| P1-05 | 대기 | **받아쓰기 확인과 분석 실패 UX.** ASR 불일치를 발음 오류 대신 전사 확인으로 안내한다. 불확실한 전사 근거의 확정 교정을 보류하고, 피드백 실패 때 답변을 유지하며 실패한 분석만 재시도한다. 없는 타임스탬프를 만들지 않는다. 레거시 피드백은 당시 기준임을 표시한다. |
| P1-06 | 대기 | **시도 관계와 도움 이력.** 질문 ID/유형, 시도 ID/부모 ID, 첫 답변·재답변·새 질문·복습 구분, 목표 ID, 도움 사용 여부를 저장한다. 기존 회차를 열 수 있어야 하고 첫 발화와 AI 수정본을 혼동하지 않으며 원본을 덮어쓰지 않는다. |
| P1-07 | 대기 | **짧은 교정과 문항별 재답변.** 선택적 자기 점검→내용 질문/힌트→뼈대/짧은 예시→같은 질문 재답변 동선을 만든다. 재답변 기본 화면은 질문과 목표만 제공하고 도움 열람을 기록한다. 건너뛰기·중단/재개를 허용하며 공통 60초 제한이나 예문 낭독 의무를 만들지 않는다. |
| P1-08 | 대기 | **선택한 목표의 변화 확인.** 사용자 첫 발화와 실제 재발화의 근거를 짝지어 목표 적용/미적용/보류를 보여 준다. 재답변 완료를 성공으로 단정하거나 AI 수정본을 재발화로 쓰지 않는다. 목표와 무관한 새 문제는 접고 다른 질문 적용/오늘 마치기로 연결한다. |
| P2-01 | 대기 | **음성만 있는 답변 회복.** 녹음이 있고 전사가 비었으면 미답변 대신 저장/전사 처리 상태를 보여 준다. API는 음성을 먼저 전사해 회복하고 재처리 경로를 제공한다. 통계·배치 분석도 음성만 있는 시도를 빠뜨리지 않는다. 텍스트만 있는 답변은 내용·표현 분석을 계속 허용한다. |
| P2-02 | 대기 | **로컬 음성 영구 저장.** 오디오와 전사를 분리해 현재 브라우저에 저장한다. 저장 완료/실패, 용량 한계, 개별·일괄 삭제, 보관 기간과 설정을 제공한다. 화면 새로고침·재방문 후 재생 및 삭제를 검증하고 사용자 허락 없이 클라우드 저장을 추가하지 않는다. |
| P2-03 | 대기 | **모바일 녹음 경로.** 브라우저 실시간 받아쓰기와 동시 녹음을 전제하지 않는 한 스트림 녹음→전사 흐름을 검토·구현한다. 권한 거부·중단·기기 전환·기존 데스크톱 경로를 점검한다. 실제 iOS/Android 녹음 검증은 장비가 없으면 별도 대기 항목으로 남긴다. |
| P2-04 | 대기 | **전사 원본·확인본과 근거 출처.** 오디오 참조, 전사 원본/확인본, 사용자 수정 이력(실제 발화 복원/새 문장 작성), 입력 방식과 분석 가능 범위를 보존한다. 작성 답변을 즉흥 말하기 성취와 분리한다. 타임스탬프는 실제 음성 처리 결과가 있을 때만 쓰고 나머지는 텍스트 위치로 표시한다. |
| P3-01 | 대기 | **새 질문으로 전이.** 같은 기능의 다른 질문을 예문 없이 제공하고 목표와 시도를 연결한다. 동일 질문 반복과 새 질문 첫 답변을 구별하며, 도움 없는 독립 답변에서 실제 목표 수행 여부를 평가한다. 수행 기회가 없으면 성공으로 세지 않는다. |
| P3-02 | 대기 | **간격 복습과 기능별 저장 카드.** 목표·기능 태그에 연결된 연습 카드를 만들고 다음 복습 시점과 성공/실패 이력을 저장한다. 다음날/일주일 뒤를 조정 가능한 초기 일정으로 사용한다. 기존 별표 저장을 유지하고 읽기/건너뛰기를 말하기 적용 완료로 기록하지 않는다. |
| P3-03 | 대기 | **홈·일상 연습·모의고사.** 홈에 오늘 다시 말할 목표 1~3개를 제안하고 단일 문항 코칭을 쉽게 시작한다. 기존 탐색을 유지한다. 실전 모드에서는 시험 중 코칭·예문 없이 진행하며 종료 후 반복 패턴과 대표 문항을 안내한다. |
| P3-04 | 대기 | **주간 기록과 학습 지표.** 목표 적용·새 질문 독립 재사용·지연 유지·과제 범위를 근거와 분모로 보여 준다. 같은 질문 반복·새 질문·도움 사용·작성 답변·기술 실패를 구분하고 적은 표본을 과장하지 않는다. 활동량과 학습 성취를 구별한다. |
| V-01 | 대기 | **통합 회귀·접근성 확인.** 과거 기록/피드백, 신규 답변→교정→재답변→전이→복습, 음성/전사/피드백 실패, 저장/삭제, 리포트/PDF 경로를 확인한다. 모바일 안전 영역·키보드 초점·44px 터치 영역·읽기 크기·좁은 화면을 점검하고 테스트/타입 검사/빌드를 통과시킨다. |
| V-02 | 대기 | **효과·음성 진단 검증 준비.** 검토 문서에 맞춰 실제 사용자 관찰, 조건을 맞춘 첫 답변·직후·지연 새 질문, 블라인드 사람 평가, ASR 오교정·이탈·부담 측정용 절차와 기록 양식을 저장소에 마련한다. 실제 참여자/기기/오디오 자료가 없으면 해당 검증은 대기로 표시한다. 검증되지 않은 발음 점수·등급 추정·100점/레이더 총점을 도입하거나 효과를 입증했다고 쓰지 않는다. |

## 현재 항목의 재개 메모

- P1-01의 시작 파일: `src/lib/feedback.ts`, `src/app/api/feedback/route.ts`, 관련 `tests/`.
- 관찰: `FREE_OPENING_TYPES`에 `roleplay_experience`가 포함되어 전화 역할극과 같은 분기를 사용한다. 단순 목록 수정으로 끝내지 말고 API의 유형별 안내와 사용자 표시까지 영향 범위를 확인한다.
- 데이터 구조가 커지는 P1-02부터는 한 실행에 감당 가능한 하위 단위로 먼저 분할한다. 레거시 읽기·신규 쓰기·렌더링은 중간 커밋에서도 호환되어야 한다.
- 외부 의존 검증은 독립 구현을 막지 않게 별도 기록하고, 가능하지 않은 실제 기기/사람 검증을 완료로 체크하지 않는다.

## 실행 기록

### 2026-09-11 — 자동 실행 준비

- 기준 문서, 현재 브랜치, `origin` 주소, 표준 검사 스크립트를 확인했다.
- 기존 사용자 문서는 수정하지 않고 구현 체크리스트를 별도 파일로 작성했다.
- `codex/feedback-learning-loop`를 기존 HEAD에서 생성하고 `origin/main`을 이력 보존 병합했다.
- 사용 한도 87%를 확인하여 제품 구현은 시작하지 않았다. 첫 구현 항목은 P1-01이다.
- 자동화 `yumi-opic`을 현재 작업에 연결하여 생성했다. 저장된 설정에서 1시간 간격, 활성 상태, 현재 작업 연결을 확인했다.
- 문서 준비 단계에서는 제품 코드가 바뀌지 않아 테스트·빌드는 실행하지 않는다. 커밋 전 문서와 공백 오류를 검토한다.
- 이 문서와 원본 검토 문서를 첫 체크포인트로 커밋·푸시한다. 원격 동기화 성공 여부는 로컬 HEAD와 원격 브랜치 SHA를 비교해 확인하고, 다음 실행은 그 상태부터 확인한다.
