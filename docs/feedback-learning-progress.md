# 피드백·학습 흐름 개선 진행 기록

기준 문서: [2026-09-11 검토](feedback-learning-review-2026-09-11.md).
사용자 요청: 이번 범위는 **연습 후 결과(채점) 화면**으로 좁힌다. 항목을 하나씩 구현하고, 1시간 간격으로 재개하며, 검증한 변경을 `KSH0660/Yumi-Opic`에 푸시한다.

## 재개 지점

- 상태: **S-01 ~ S-05 완료. S-04는 되돌렸고 S-02는 고정 눈금으로 바꿨다. 푸시함**.
- 이번 범위: 연습 후 결과(채점) 화면과 그 화면을 채우는 피드백 생성. 아래 `범위 밖(보류)` 표의 항목은 건드리지 않는다.
- 다음 항목: 없음. S-01 ~ S-05를 모두 마쳤다. 다음 범위는 `범위 밖(보류)` 표에서 사용자와 다시 고른다. 아래 순서대로 진행하며, 이미 진행 중인 항목이 있으면 그것부터 끝낸다.
- 작업 디렉터리: 현재 실행 환경의 저장소 루트. 원격 세션에서는 `/home/user/Yumi-Opic`이다.
- 작업 브랜치: `claude/doc-folder-plan-file-a2g5k3`. 이전 기록의 `codex/feedback-learning-loop`는 `origin/main`에 병합되어 종료되었다.
- 원격: `origin` = `https://github.com/KSH0660/Yumi-Opic`.
- 기준 커밋: `651afd7` (`origin/main`). 검토 문서와 이 진행 기록은 이미 `main`에 있다.
- 자동화: 이전 기록의 `yumi-opic` heartbeat는 다른 도구에서 만든 것이라 현재 환경에서 상태를 확인할 수 없다. 이 환경의 1시간 루프는 별도로 설정한다.
- 현재 구현 변경: S-01·S-02·S-03·S-05. `ExamResult.tsx`, `api/feedback/route.ts`, `lib/cost.ts`, `README.md`. S-04는 되돌려 남은 코드가 없다.
- 미커밋 구현 / 실패한 검사 / 미푸시 커밋: 없음.

## 매 실행의 규칙

1. 먼저 사용량 도구로 계정의 현재 한도를 확인한다. `rateLimitsByLimitId`가 있으면 그것을 우선하고 사용 중인 모델에 적용되는 모든 창을 살핀다. 5시간 등 단기 창이 85% 이상이거나 주간 창이 90% 이상이면 새 구현을 시작하지 않는다. 사용량 조회 불가·한도 도달·지출 제한도 작업 확대 없이 체크포인트 후 종료한다. 다음 정기 실행에서 다시 확인한다. 사용량이 회복될 때까지 장시간 sleep, 바쁜 재시도, 즉시 반복 실행을 하지 않는다. 리셋 크레딧 사용·추가 구매·모델 변경은 자동으로 하지 않는다.
2. 여유가 있으면 이 파일의 재개 지점, 기준 문서의 해당 절, 적용되는 저장소 지침과 필요한 코드만 읽는다. 전체 조사를 매번 반복하지 않는다. 한 실행에 **미완료 항목 하나 또는 그 안의 작은 검증 가능한 단위 하나**만 진행하고 종료한다. 큰 항목은 먼저 아래 표에 세부 단위를 적어 여러 실행으로 나눈다.
3. 시작 전에 상태를 `진행 중`으로 기록하고, 범위·예정 수정 파일·완료 조건을 적는다. 약 25분의 작업 시점 또는 큰 변경 직후에 사용량을 다시 확인해 정리 시간을 남긴다. 실제 안전한 중단점까지 유연하게 정리하되, 여유가 줄면 새 하위 작업을 시작하지 않는다. 모델이 중단되더라도 작업 파일과 이 기록에서 복구할 수 있어야 한다.
4. 사용자 변경을 보존한다. `git status`, 현재 브랜치, remote URL을 먼저 확인하고, 기록되지 않은 수정은 임의로 스테이징·삭제·덮어쓰기하지 않는다. 다른 작업과 충돌하면 분리된 작업 공간에서 진행할 수 있는지 판단하고, 해결할 수 없는 충돌만 기록한다. 중복 실행·이미 진행 중인 동일 작업을 시작하지 않는다.
5. 푸시 전 `git fetch origin`으로 원격 상태를 확인한다. 작업 브랜치 원격 갱신이 있으면 이력을 보존하여 통합하고 관련 검증을 다시 한다. 강제 푸시, 원격 브랜치 삭제, 사용자 작업 초기화는 하지 않는다. `upstream`에는 푸시하지 않는다. `main` 병합·서비스 배포는 별도 요청이 없는 한 하지 않는다.
6. 동작 변경에는 의미 있는 회귀 검증을 포함한다. 저장소 표준은 `npm test`, `npm run typecheck`, `npm run build`이다. 빌드와 타입 검사는 생성 파일 경합을 피하도록 순차 실행한다. 문서만 바꾼 실행에는 `git diff --check`와 문서 검토면 충분하다. UI 변경은 해당 UI 스킬을 적용하고 가능한 로컬 브라우저에서 휴대폰/데스크톱 크기·키보드·레이아웃을 확인한다. 실제 모바일 마이크, 유료 API, 사람 평가를 수행하지 못했으면 그 한계를 그대로 남긴다.
7. 성공한 단위를 관련 파일과 이 진행 기록만 명시적으로 스테이징하여 커밋한다. `git push -u origin codex/feedback-learning-loop`로 푸시하고 원격 브랜치 SHA가 로컬 HEAD와 같은지 확인한다. 미완성·검증 실패 코드는 완료로 표시하거나 정상 변경으로 푸시하지 않는다. 중단된 변경은 로컬 파일과 아래 재개 메모로 보존한다. 푸시 실패 시 검증된 로컬 커밋을 보존하고 다음 실행에서 먼저 푸시를 재시도한다.
8. 종료 전 재개 지점·완료/진행 상태·검증·수정 파일·다음 행동·미푸시 여부를 갱신한다. 커밋 자체의 SHA는 그 커밋 안에 넣으려 하지 말고 다음 실행에서 기록하거나 `git log`로 확인한다. 기다리는 상태가 똑같으면 불필요한 문서 커밋과 알림을 만들지 않는다.
9. 사용자에게는 항목 완료와 푸시, 새로운 실패, 필요한 실제 기기/사용자 확인, 전체 완료 같은 의미 있는 변화만 알린다. 동일한 사용량 대기는 조용히 끝낸다.
10. 모든 구현과 검증·푸시가 끝나면 최종 결과를 기록하고 이 heartbeat를 도구로 일시정지한다. 사람 평가나 실제 기기가 없어서 독립 작업이 더 이상 불가능한 경우에도 그 항목을 `외부 검증 대기`로 남기고 한 번 알린 뒤 일시정지한다. 대기를 전체 완료로 바꾸지 않는다. 재개 요청을 받으면 같은 자동화를 갱신한다.

위 사용량 임계값과 실행 길이는 이 작업의 보수적인 운영 기준이다. 토큰을 정확히 강제 제한하는 장치는 아니며, 계정 한도나 앱 실행 상태로 정기 실행이 지연될 수 있다. 로컬 파일을 쓰는 자동 실행에는 컴퓨터와 Codex 앱이 실행 중이어야 한다.

## 구현 체크리스트

**범위: 연습 후 결과(채점) 화면.** 대상 파일은 `src/components/ExamResult.tsx`, `src/components/FeedbackDetails.tsx`, `src/app/api/feedback/route.ts`, `src/lib/feedback.ts`, `src/lib/types.ts`와 관련 `tests/`이다.

### 사용자와 확정한 설계 결정

아래는 검토 문서의 권고를 사용자 판단으로 바꾼 것이다. 구현 중 이 결정을 되돌리지 않는다.

1. **두괄식 판정은 유지한다.** 검토 문서는 `삭제·교체`로 적었으나 OPIc 대비가 목표이므로 그대로 둔다. 현재 `requiresFrontLoadedOpening()`의 유형별 기준도 유지한다.
2. **상단 카드에 회차 전체를 판정하는 요약을 넣지 않는다.** `오늘 고칠 것 1개` 같은 집계 목표는 만들지 않는다. 피드백은 문항별로만 본다.
3. **문항 카드 안의 고칠 점 5개 나란히 표시는 그대로 둔다.** 1개만 펼치고 접는 재구성은 하지 않는다.
4. **AI 피드백 자동 시작은 하지 않는다.** 버튼을 누르는 행위가 API 비용 통제 장치다. 문항당 최대 30~50원, 15문항이면 회당 최대 750원이다.
5. **짧은 답변을 자동 제외하거나 `짧아요`로 표시하지 않는다.** 건너뛰거나 짧게 답한 데는 사용자 사정이 있다. 시간만 보여주고 판단은 사용자가 한다.
6. **말한 시간을 색으로 판정하지 않는다.** 막대는 길이와 상관없이 브랜드색 한 가지다. 적정 구간만 트랙에 음영으로 표시하고 판단은 사용자가 한다. 짧은 쪽·긴 쪽에 다른 색을 입히는 안(노랑·주황, 초록, 인디고 3단계)을 모두 시안으로 그려 본 뒤 내린 결정이다.
7. **상단 카드에 회차 집계를 넣지 않는다.** 항목별 보강 문항 수를 모아 보는 것도 하지 않는다. 다시 연습하러 온 사람은 어차피 고칠 게 있고, 종합 통계는 정신만 사납다. 상단 카드는 제목·날짜·요약 한 줄·타임라인·접힌 두 줄로 끝낸다.
8. **연결 표현을 코드로 세어 프롬프트에 넘기지 않는다.** 표현 목록만 프롬프트에 넣고 어디가 반복인지는 모델이 읽어서 판단한다. 상단 카드에 `쓴 연결 표현` 집계도 넣지 않는다.

`대기 → 진행 중 → 완료`로 바꾼다. 한 실행에 항목 하나만 진행한다.

| ID | 상태 | 범위와 완료 조건 |
| --- | --- | --- |
| S-01 | 완료 | **상단 카드 접기.** 설명문 6덩어리(안내 2개, 미답변 안내, 다시 받아쓰기 안내, 단어 계산 기준, 코칭 방식 7줄)와 숫자 9개 그리드를 `<details>`로 접는다. 요약 줄은 `연습 통계`, `저장·분석 안내` 두 개다. `같은 문제 다시 풀기`·`문제 다시 뽑기`·`다음` 버튼 세 개는 문항 목록 아래로 내린다. 남는 것은 배지·제목·날짜·`답변 N문항 · 시간 · 녹음 N개` 한 줄이다. 접힌 내용은 하나도 지우지 않는다. |
| S-02 | 완료 (기준 변경) | **문항별 진행바 타임스탬프.** 답변한 문항만 순서대로 `번호 · 유형 · 주제`와 말한 시간을 보여 주고, 그 회차 최장 답변을 가득 찬 것으로 잡아 `ProgressBar`(`ui.tsx`)로 그린다. 경고 문구·아이콘·`짧아요` 표시를 붙이지 않는다. 답변 없는 문항은 지금처럼 뺀다. 시간은 이미 있는 `times[slot]`을 쓴다. |
| S-03 | 완료 | **discourse marker 프롬프트 강화.** `route.ts`의 막연한 transition 지시를 아래 10기능 표현 목록으로 교체한다. 같은 마커가 반복되면(예: `so`) 같은 기능 안의 다른 표현으로 바꿔 제안하게 하고, 뜻이 달라지는 다른 기능의 표현을 제안하지 않게 한다. 표현을 억지로 채워 넣으라고 하지 않는다. 유형별 회귀 검증을 확인한다. |
| S-04 | 되돌림 | **보강 항목 집계.** 피드백을 받은 뒤 상단 카드에 카테고리별 보강 문항 수를 분모와 함께 보여 준다(예: `두괄식 3문항 / 5문항 기준`). 아직 분석하지 않은 문항이 있으면 분모와 함께 그 수를 밝힌다. 이것은 집계이며 `오늘 고칠 것`으로 단정하지 않는다. 피드백이 없으면 이 영역을 그리지 않는다. |
| S-05 | 완료 | **회귀·접근성 확인.** 레거시 피드백 열람, 신규 답변→피드백→상세, 실패·재시도, 별표 저장, 리포트·PDF 경로를 확인한다. 모바일 안전 영역·키보드 초점·44px 터치 영역·좁은 화면을 점검하고 `npm test`, `npm run typecheck`, `npm run build`를 통과시킨다. |

### S-03에 넣을 표현 목록

앞의 7개는 사용자가 준 분류이고, 뒤의 3개는 경험 서술 문항을 위해 추가했다. 쉬운 구어체만 쓴다.

| 기능 | 표현 |
| --- | --- |
| 이유 | because · because of + 명사 · since · the reason is (that) ~ · mainly because · that's partly because |
| 결과 | so · as a result · because of that · that's why · which means · in the end |
| 추가 | also · and also · on top of that · besides that · another thing is (that) ~ · plus · what's more |
| 전환 | anyway · speaking of that · when it comes to ~ · as for ~ · talking about ~ · by the way · moving on to ~ |
| 강조·포인트 | the thing is ~ · what's really nice is ~ · what I like most is ~ · the best part is ~ · what really matters is ~ · to be honest · actually |
| 예시 | for example · for instance · like · such as · things like ~ · say, ~ · let me give you an example |
| 대조 | but · however · on the other hand · although · even though · still · at the same time · then again |
| 시간·순서 | first (of all) · then · after that · at first · while I was ~ing · later on · finally |
| 마무리 | so that's pretty much it · overall · all in all · that's why I ~ · anyway, that's about it · in short |
| 의견·태도 | I think · I'd say · for me · personally · if you ask me · I feel like · in my opinion |

### 범위 밖(보류)

결과 화면 작업이 끝난 뒤 다시 판단한다. 원래 ID는 이전 체크리스트 기준이다.

| 보류 항목 | 원래 ID |
| --- | --- |
| 버전 있는 피드백 데이터와 레거시 읽기 | P1-02 |
| 질문별 피드백 생성 기준 교체, 목표 1개 | P1-03 |
| 역할극 관련 경험 분리 | P1-01 |
| 시도 관계·도움 이력, 짧은 교정, 재답변, 변화 확인 | P1-06 ~ P1-08 |
| 받아쓰기 확인 전환, 분석 실패 UX | P1-05 |
| 음성만 있는 답변 회복, 로컬 음성 저장, 모바일 녹음, 전사 원본 보존 | P2-01 ~ P2-04 |
| 새 질문 전이, 간격 복습, 홈·모의고사, 주간 기록 | P3-01 ~ P3-04 |
| 효과·음성 진단 검증 준비 | V-02 |

### 2026-09-12 — R-02 2단 렌더링

- `FeedbackDetails.tsx` 를 다시 썼다. After 를 위에 두고 Before 를 `<details>` 로 접었다. 모아보기·인쇄는 `variant="report"` 로 Before 를 펼치고 연한 등급도 처음부터 그린다. 접힌 내용과 버튼은 종이에 남지 않기 때문이다.
- 연한 등급은 기본으로 끈다. `다듬은 곳 N군데 보기` 버튼을 누를 때만 점선 밑줄로 그린다. 끈 상태에서는 `del` 의 기본 취소선까지 지워 평범한 글자로 만든다.
- 진한 등급에 항목 번호를 위 첨자로 달았다. 번호나 고칠 점 위에 손을 올리면 양쪽이 함께 밝아진다. 마우스만 쓴다. 본문 한가운데의 조각에 `tabIndex` 를 뿌리면 키보드 이동이 망가진다.
- **갈린 조각을 이어 붙인다.** R-01 에서 본 대로 한 항목이 초록 세 덩이로 갈렸다. 사이에 낀 것이 바뀌지 않은 말뿐일 때만 잇는다. 다른 등급의 변경을 만나면 멈춘다. Before 에는 쓰지 않는다. 빼지 않은 말에 취소선을 그으면 거짓말이 된다.
- 이은 뒤에도 조각마다 `px-0.5` 패딩이 붙어 이음매가 보였다. 같은 등급·같은 번호가 이어지면 한 요소로 합쳐서 그리도록 `panelGroups()` 를 뒀다.
- `고친 곳 N군데` 를 `고칠 점 N가지 반영 · 다듬은 곳 M군데` 로 바꿨다. 앞의 수는 `locatedItems.length` 다. 틈 단위로 세면 항목 3개가 5군데로 보인다.
- 검증: `npm test` 162개 통과, `npm run typecheck`, `npm run build` 통과. 프로덕션 빌드에 조깅·습관 회차를 심어 390px 라이트(기본·다듬은 곳 켬·Before 펼침), 900px 다크, `/report` 900px 다섯 화면을 실제로 렌더링해 확인했다. 콘솔 에러·가로 스크롤 없음.
- 확인하지 못한 것: 실제 OpenAI 응답으로 만든 인용문. 이 환경에 API 키가 없어 인용문은 손으로 만든 값으로 확인했다. 실제 매칭 실패율은 사용자가 한 번 돌려 봐야 안다.

### 2026-09-12 — R-03a 따라 읽기 재생기

- 사용자 결정으로 **읽을 단위를 문장에서 After 전체로 바꿨다.** 설계 결정 4번을 고쳐 적었다.
- `src/lib/readAloud.ts` 를 새로 뒀다. 속도 범위·자르기·저장·낱말 쪼개기가 여기 있고 테스트 9개를 붙였다(162 → 171개). 컴포넌트에는 상태와 그리기만 남겼다.
- **표시는 배경과 글자색만 바꾼다.** 안팎 여백을 주면 표시가 옮겨 갈 때마다 글자가 밀려 읽던 자리를 잃는다. 브라우저에서 마지막 낱말의 좌표와 문단 높이를 재생 전후로 재어 밀리지 않는 것을 확인했다.
- 낱말마다 조각을 두되 등급 배경은 **묶음 쪽에** 남겼다. 낱말마다 배경을 주면 R-02 에서 없앤 이음매가 되돌아온다.
- 속도는 분당 낱말 수(60~180, 10 단위)다. 앱이 전달력을 보는 단위와 같아 숫자가 그대로 뜻이 된다. 고른 값은 `yumi-opic:read-wpm` 으로 남긴다. 서버가 그린 첫 화면과 어긋나지 않도록 `useEffect` 에서 읽는다.
- 낱말마다 `setTimeout` 을 다시 건다. 읽는 도중 속도를 바꾸면 바로 다음 낱말부터 따른다.
- 멈추면 표시가 남고(읽던 자리를 찾아야 한다) 다 읽으면 거둔다. 화면 밖으로 나간 낱말은 `scrollIntoView({ block: "nearest" })` 로 끌어온다. 이미 보이면 아무것도 하지 않는다.
- 좁은 화면에서 속도 줄이 `ml-auto` 때문에 혼자 오른쪽으로 밀려 어정쩡하게 들여쓰였다. 390px 에서는 한 줄을 다 쓰고 왼쪽부터 채우게 했다.
- 검증: `npm test` 171개 통과, `npm run typecheck`, `npm run build` 통과. 프로덕션 빌드에서 390px 라이트(대기·재생·멈춤·속도 변경), 900px 다크 재생, 끝까지 재생 후 `다시 읽기` 로 바뀌는 것까지 실제로 확인했다. 글자 밀림·콘솔 에러·가로 스크롤 없고 속도가 저장된다.
- 남은 것: 읽은 횟수 기록(R-03b). 재생기는 완주 여부를 이미 알고 있어 `ExamResult` 까지 콜백만 이으면 된다.

### 2026-09-12 — R-03b·R-04 읽은 횟수와 마이크 확인

- 끝까지 따라 읽으면 `SavedResult.readCounts` 에 한 번으로 센다. 답변·피드백과 같은 저장 경로(`persist`)에 얹었고, 지난 기록을 열어 읽어도 `readOnly` 를 풀어 그 회차에 쌓는다. 예전 기록에는 키를 만들지 않는다(`browserAnswers` 와 같은 방식).
- `마이크로 확인` 을 켜면 읽는 동안 `startDictation()` 이 함께 돈다. 결과 화면에는 녹음이 없어 `micShare` 마이크 경합이 없다.
- 커버리지는 순서를 지킨 채 겹치는 낱말(최장 공통 부분열) ÷ 읽을 글의 낱말 수다. 건너뛴 말은 빠지고 받아쓰기가 끼워 넣은 엉뚱한 말은 세지 않는다. 대소문자·문장부호는 보지 않는다.
- **통과 조건으로 쓰지 않는다(설계 결정 6번).** 70% 를 못 넘겨도 횟수는 그대로 세고 `조금 또렷하게 읽어 보세요` 한 줄만 붙인다. 브라우저 받아쓰기는 제대로 읽어도 틀린다.
- 커버리지는 `stop()` 뒤의 `onEnd` 에서 센다. 말하던 마지막 문장이 그때 확정되기 때문이다. 마이크가 막힌 채 끝났으면 0% 를 들이밀지 않는다. 읽지 않은 것이 아니다.
- 검증: `npm test` 177개 통과(6개 추가), `npm run typecheck`, `npm run build` 통과. 프로덕션 빌드에서 세 경우를 실제로 돌렸다 — 마이크 없이 완주(`읽음 1회`, `readCounts` 저장 확인), 거의 그대로 읽음(100%), 많이 건너뜀(27% + 안내, 횟수는 그대로 1회). 마이크 확인 설정도 저장된다.
- **확인하지 못한 것**: 진짜 음성 인식이다. 이 환경의 헤드리스 크로미움에는 음성 서비스가 없어 `SpeechRecognition` 이 바로 오류를 낸다. 그래서 같은 인터페이스를 흉내 낸 인식기를 주입해 붙는 부분만 확인했다. 실제 기기에서의 인식률은 사용자가 써 봐야 안다. 이 크로미움은 `SpeechRecognition` 과 `webkitSpeechRecognition` 을 **둘 다** 갖고 있어, 흉내 낸 인식기를 하나만 덮으면 실제 엔진이 잡힌다.

## 현재 항목의 재개 메모

- 화면 확인 방법: `npm run build && npx next start -p <포트>` 로 띄우고, `localStorage`의 `yumi-opic:history` 에 `SavedResult` 형태의 회차를 심은 뒤 `/exam?history=<id>` 로 들어가면 피드백까지 채운 결과 화면을 볼 수 있다. `.test-build/lib/exam.js` 의 `buildRandomPractice()` 로 시험 객체를 만들면 된다.
- 브라우저는 `/opt/pw-browsers/chromium` 을 `playwright-core` 의 `executablePath` 로 지정해 쓴다. 저장소에는 Playwright 의존성이 없다.
- 남은 확인: 실제 마이크로 새 연습을 끝까지 돌린 경로와 실제 iOS/Android 기기는 이 환경에서 확인하지 못했다.
- 실제 화면 확인은 S-05에서 한 번에 한다. 저장소에 컴포넌트 테스트가 없어 `npm test`로는 레이아웃을 검증할 수 없다. 브라우저로 띄우려면 `localStorage`에 연습 기록을 심고 지난 기록 열람 경로로 들어가야 한다.
- S-02의 시간 값은 `times[slot]`, 렌더는 `formatTime()`과 `ProgressBar`를 쓴다. 둘 다 이미 있다.
- S-04는 `feedbackBySlot`의 각 `items[].category`를 세면 된다. `FEEDBACK_CATEGORIES`와 `feedbackCategoryLabel`(`lib/feedback.ts`)을 그대로 쓴다.
- 지난 기록 열람 경로에서도 같은 화면을 쓰므로 `historyEntry`가 있는 경우를 매번 함께 확인한다.

## 실행 기록

### 2026-09-11 — 자동 실행 준비

- 기준 문서, 현재 브랜치, `origin` 주소, 표준 검사 스크립트를 확인했다.
- 기존 사용자 문서는 수정하지 않고 구현 체크리스트를 별도 파일로 작성했다.
- `codex/feedback-learning-loop`를 기존 HEAD에서 생성하고 `origin/main`을 이력 보존 병합했다.
- 사용 한도 87%를 확인하여 제품 구현은 시작하지 않았다. 첫 구현 항목은 P1-01이다.
- 자동화 `yumi-opic`을 현재 작업에 연결하여 생성했다. 저장된 설정에서 1시간 간격, 활성 상태, 현재 작업 연결을 확인했다.
- 문서 준비 단계에서는 제품 코드가 바뀌지 않아 테스트·빌드는 실행하지 않는다. 커밋 전 문서와 공백 오류를 검토한다.
- 이 문서와 원본 검토 문서를 첫 체크포인트로 커밋·푸시한다. 원격 동기화 성공 여부는 로컬 HEAD와 원격 브랜치 SHA를 비교해 확인하고, 다음 실행은 그 상태부터 확인한다.

### 2026-09-12 — 범위 재조정

- 사용자 결정으로 구현 범위를 연습 후 결과(채점) 화면으로 좁혔다. 우선 2·3과 별도 화면이 필요한 항목은 `범위 밖(보류)`로 옮겼다.
- 사용자 지적에 따라 두괄식 판정을 유지하기로 했다. 검토 문서의 `삭제·교체` 권고를 `유지·조정`으로 바꾸고, 강제 표시와 고정 우선순위만 없애는 것으로 항목을 다시 썼다.
- 19개 항목을 결과 화면 기준 11개 하위 단위(R-01a ~ R-05)로 다시 나눴다. 한 실행에 하나씩 진행한다.
- 작업 브랜치를 `claude/doc-folder-plan-file-a2g5k3`으로 바꿨다. 이전 `codex/feedback-learning-loop`는 `origin/main`에 병합되어 종료되었다.
- 문서만 바꾼 실행이므로 테스트·빌드는 실행하지 않는다. 커밋 전 `git diff --check`와 문서 검토를 한다.

### 2026-09-12 — 결과 화면 범위 확정

- 사용자와 상의해 범위를 5개 항목(S-01 ~ S-05)으로 확정했다. 이전 R-01a ~ R-05 안은 폐기하고 보류 표로 옮겼다.
- 확정한 설계 결정 6가지를 체크리스트 앞에 적었다. 자동 피드백 시작, 짧은 답변 자동 제외, 마커 로컬 집계, 회차 단위 목표 요약, 문항 카드 재구성은 모두 사용자 판단으로 하지 않기로 했다.
- discourse marker 10기능 표현 목록을 문서에 넣어 S-03에서 그대로 쓰게 했다.
- 다음 항목은 S-01이다.

### 2026-09-12 — S-01 상단 카드 접기

- `src/components/ExamResult.tsx`의 상단 `Card`에서 설명문 6덩어리와 숫자 9개 그리드를 `<details>` 두 개(`연습 통계`, `저장·분석 안내`)로 접었다. 내용은 하나도 지우지 않았다.
- 카드에 남긴 것: 배지, 제목, 날짜, `답변 N문항 · 말한 시간 · 녹음 N개` 한 줄, 그리고 저장 실패 알림.
- `saveError`는 접지 않고 카드 본문에 그대로 뒀다. 저장 안내는 `저장·분석 안내` 안으로 옮겼다.
- `같은 문제 다시 풀기`·`문제 다시 뽑기`·`다음` 버튼 묶음을 문항 목록 아래로 내렸다. 피드백을 보기 전에 문제를 다시 뽑을 일이 없기 때문이다.
- `BatchFeedbackPanel`은 옮기지 않았다. 상단 카드가 짧아져 이미 첫 화면에 들어온다.
- 검증: `npm test` 152개 통과, `npm run typecheck` 통과, `npm run build` 통과. 컴포넌트 테스트는 저장소에 없어 실제 화면은 다음 실행에서 브라우저로 확인한다.
- 다음 항목은 S-02(문항별 진행바 타임스탬프)다.

### 2026-09-12 — S-02 문항별 진행바 타임스탬프

- `AnswerTimeline` 컴포넌트를 `ExamResult.tsx`에 추가하고 상단 카드의 `답변 N문항 · 말한 시간 · 녹음 N개` 줄 바로 아래에 넣었다.
- 답변한 문항만(`answeredSlots`) 순서대로 번호·유형·주제와 말한 시간을 보여 주고, 막대는 그 회차 최장 답변을 가득 찬 것으로 잡은 상대값이다.
- 경고 문구·아이콘·`짧아요` 표시를 넣지 않았다. 확정한 설계 결정 5번에 따른다.
- 번호는 문항 카드와 같은 `itemNumber()` 값을 쓴다. 건너뛴 문항이 있어도 두 목록이 어긋나지 않는다. `practice` 모드는 `2·5·8번`처럼 길어질 수 있어 고정 정사각형 대신 자동 너비 배지로 그렸다.
- 문항 목록의 `답변한 문항 / 전체` 필터와는 연동하지 않는다. 타임라인은 항상 답변한 문항만 보여 준다.
- 검증: `npm test` 152개 통과, `npm run typecheck` 통과, `npm run build` 통과. 실제 화면 확인은 S-05에서 한다.

### 2026-09-12 — S-03 discourse marker 프롬프트

- `route.ts`의 막연한 transition 지시 한 줄을 10기능 표현 목록과 규칙 두 개로 바꿨다. 목록은 이 문서의 `S-03에 넣을 표현 목록` 표와 같다.
- 규칙 1(반복): 같은 마커가 전환을 대부분 떠맡으면 사용자의 반복된 문장을 인용하고 **같은 기능 줄 안의 다른 표현**으로 바꿔 준다. 다른 기능의 표현으로 바꾸지 않게 막았다. 모델이 직접 읽고 판단하며, 코드로 세어 넘기지 않는다(설계 결정 6번).
- 규칙 2(누락): 관계가 분명한데 표시가 없어 끊겨 들릴 때만 그 자리에 넣게 했다. "연결 표현을 더 쓰세요" 같은 일반 조언과 안 쓴 기능을 채우라는 요구를 금지했다.
- 프롬프트 고정 분량이 늘어 `PROMPT_OVERHEAD_TOKENS`를 1,000 → 1,500으로 올렸다. 지시문 본문만 재면 약 1,250 토큰이고 유형별 분기와 라벨이 더 붙어 여유를 뒀다.
- 다시 계산한 문항당 최대 비용은 짧은 답변 36원, 보통 45원, 긴 답변 60원이다. README의 `30~50원`을 `35~60원`으로 고쳤다. 목록 추가분은 문항당 1~2원이며, 나머지 차이는 기존 표기가 상한을 낮게 잡고 있던 것이다.
- 검증: `npm test` 152개 통과, `npm run typecheck` 통과, `npm run build` 통과.

### 2026-09-12 — S-04 보강 항목 집계

- `lib/feedback.ts`에 순수 함수 `tallyFeedback()`을 넣고 `tests/feedback.test.cjs`에 테스트 2개를 더했다(152 → 154개).
- 세는 단위는 지적 횟수가 아니라 **문항**이다. 한 문항에서 문법이 두 번 나와도 한 문항으로 센다.
- 흐름 3단계는 문항 유형에 따라 첫 단계 이름이 갈리므로 `두괄식 도입`과 `요청·문제 전달`을 **다른 줄로 나눠** 각자의 분모로 센다. 롤플레이가 두괄식 분모에 섞이지 않는다.
- 모든 줄에 분모를 붙였다(`2/3문항`). 3문항 중 3개와 15문항 중 3개가 같아 보이지 않게 한다.
- 아직 분석하지 않은 문항은 분모에서 빼고 `N문항은 아직 분석하지 않았습니다`로 따로 밝힌다.
- 집계일 뿐 판정이 아니다. `오늘 고칠 것`을 고르지 않는다(설계 결정 2번).
- 피드백이 하나도 없으면 이 영역을 그리지 않는다.
- 검증: `npm test` 154개 통과, `npm run typecheck` 통과, `npm run build` 통과.

### 2026-09-12 — S-05 회귀·접근성 확인

- 프로덕션 빌드를 띄우고 `localStorage`에 3문항 회차(피드백 포함)를 심어 `/exam?history=seed-demo` 로 결과 화면을 실제로 렌더링해 확인했다. 390px와 900px 두 폭에서 봤다.
- **고친 문제 1 — 같은 이름이 두 번 보였다.** 흐름 축의 `활동·디테일`·`감정·의미`와 고칠 점 유형의 같은 이름이 한 목록에 이어 붙어 중복처럼 읽혔다. `답변 흐름`과 `고칠 점으로 나온 유형` 소제목으로 갈랐다.
- **고친 문제 2 — 터치 영역이 20px였다.** `연습 통계`·`저장·분석 안내` 요약이 44px에 못 미쳤다. `py-3`을 넣어 44px로 맞추고 `details`의 위 여백을 줄여 간격을 유지했다.
- 확인한 것: 접은 내용 8가지(고유 단어·문장 수·다시 듣기·힌트 사용·녹음본·미답변 안내·저장 안내·코칭 방식)가 펼치면 모두 살아 있음. 저장된 피드백 열람 정상(총평·고칠 점·영어 예시·흐름 배지). 키보드 첫 Tab 초점 `outline: solid 2px`. 390px에서 가로 스크롤 없음(scrollWidth 390). 콘솔 에러 없음.
- 확인하지 못한 것: 실제 마이크로 새 연습을 끝까지 돌린 경로, 실제 iOS/Android 기기, 실제 OpenAI 응답으로 생성한 연결 표현 피드백. 이 환경에 마이크·실기기·API 키가 없다.
- 검증: `npm test` 154개 통과, `npm run typecheck` 통과, `npm run build` 통과.

## 다음 범위 — 고친 문장 읽기 (R-01 ~ R-04)

S-01 ~ S-05로 결과 화면 작업을 마친 뒤, 사용자와 상의해 다음 범위를 `범위 밖(보류)` 표가 아닌 새 주제로 정했다. **고친 답변을 읽을거리로 두지 않고 소리 내어 말하게 만드는 것**이다. 지금 화면은 `After 를 소리 내어 두세 번 읽어 본 뒤…` 안내 한 줄이 전부라 눈으로 훑고 넘어간다.

### 사용자와 확정한 설계 결정

1. **항목마다 After 인용문을 받는다(방법 A).** 답변을 두 번(최소 수정본 + 자연스러운 판) 받아 기계적으로 가르는 안(방법 B)은 문항당 8원이 더 들고 항목 번호를 붙일 수 없어 쓰지 않는다. 인용문 매칭 실패율이 눈에 보이게 높으면 그때 B로 갈아탄다.
2. **읽기 목표는 문장당 1번.** 더 읽으면 횟수만 쌓인다. 3번으로 시작하지 않는다.
3. **읽은 횟수를 기록에 남긴다.** `SavedResult` 에 `readCounts` 를 더한다.
4. **읽을 단위는 After 전체다.** 처음에는 진한 등급이 든 문장만 뽑기로 했으나, 사용자 결정으로 **고친 답변 전체를 따라 읽는 것**으로 바꿨다. 문장만 뽑으면 그 문장 앞뒤의 흐름이 끊겨 읽는 맛이 없다. 목표가 1번이고 속도를 사용자가 정하므로, 250단어도 한 번 훑는 데 1분 안팎이라 무리가 아니다.
5. **연한 등급에 배경색을 주지 않는다.** 연한 배경도 얼룩으로 읽힌다. 점선 밑줄만 긋고, 그마저 끄는 토글을 둔다.
6. **받아쓰기 확인은 통과 조건이 아니다.** 브라우저 받아쓰기는 제대로 읽어도 틀리게 적는다(그래서 OpenAI 재전사 덮어쓰기 기능이 있다). 진행 표시로만 쓰고 못 알아들어도 넘어갈 수 있게 한다.

이 범위를 정하기 전에 지금 화면 / 제약만 풀었을 때 / 제안 세 가지를 실제 색 토큰으로 그린 화면 시안을 만들어 사용자가 확인했다.

| ID | 상태 | 범위와 완료 조건 |
| --- | --- | --- |
| R-01 | 완료 | **항목 ↔ After 자리 연결.** 응답에 `itemQuotes` 를 더해 항목의 `afterQuote` 로 저장한다. `improvedAnswer` 프롬프트의 최소 수정 제약을 걷고 자연스럽게 고치게 바꾼다. `diffAnswers` 가 바뀐 곳을 `item` / `fluency` 두 등급으로 가른다. 화면은 아직 건드리지 않는다. |
| R-02 | 완료 | **2단 렌더링.** 진한 등급은 지금 색에 ①②③ 배지, 연한 등급은 점선 밑줄만. Before 패널을 `<details>` 로 접는다. 연한 등급 토글을 둔다. 결과 화면과 피드백 모아보기·PDF 두 경로를 함께 확인한다. |
| R-03a | 완료 | **따라 읽기 재생기.** After 전체를 낱말 단위로 흘려 보낸다. 재생·멈춤·처음부터와 속도 조절(분당 60~180, 브라우저에 저장)을 둔다. 표시는 글자를 밀지 않아야 한다. |
| R-03b | 완료 | **읽은 횟수 기록.** 끝까지 읽으면 한 번으로 세어 `SavedResult.readCounts` 에 남긴다. |
| R-03c | 안 함 | **`speak()` 들려주기.** 사용자 결정으로 하지 않는다. |
| R-04 | 완료 | **받아쓰기 확인.** `startDictation()` 으로 커버리지를 재어 70% 아래면 한 줄만 알린다. 통과 조건으로 쓰지 않는다. |

### R-01 에서 확인한 것 (R-02 에 반영함)

조깅·습관 문항의 실제 답변 한 개로 확인한 결과다.

- **한 항목이 여러 조각으로 쪼개진다.** 긴 문장을 고치면 그 안의 단어가 예전 답변과 부분적으로 맞아 조각이 갈린다(감정·의미 항목 하나가 3조각). **번호 배지는 항목의 첫 조각에만 그려야 한다.**
- **"고친 곳 N군데"는 `locatedItems.length` 로 세야 한다.** 틈 단위로 세면 항목 3개가 5군데로 보인다.
- **연한 등급이 예상보다 훨씬 많다.** 같은 답변에서 13군데가 나왔다. 사용자 결정으로 **연한 등급은 기본으로 끈다.** 실제로 켜 본 화면에서도 점선 13개는 얼룩으로 읽혔다.
- **말을 빼기만 한 항목은 번호를 못 받을 수 있다.** 인용문은 After 에서 떠 오는데 삭제는 Before 쪽에만 남는다. 인용문이 걸친 틈이 순수 삭제면 거기에 붙이지만, 그 틈에 새로 넣은 말이 섞여 있으면(주변 문장이 함께 다듬어진 경우) 잘못 가리키지 않기 위해 포기한다. 확인한 답변에서 연결 표현 항목이 그랬다.

### 2026-09-12 — S-04 되돌림

- 사용자 확인 결과 상단 카드의 `보강이 나온 항목` 집계가 정신 사납다고 판단해 전부 걷어냈다. 다시 연습하러 온 사람은 어차피 고칠 게 있으므로 회차를 종합해 보여 줄 이유가 없다.
- 지운 것: `ExamResult.tsx`의 `FeedbackTallyView`·`TallyRow`와 집계 계산, `lib/feedback.ts`의 `tallyFeedback()`·`FeedbackTally`·`FeedbackTallyRow`, `tests/feedback.test.cjs`의 집계 테스트 2개(154 → 152개). 쓰는 데가 없어 죽은 코드로 남기지 않았다.
- S-05에서 집계 때문에 넣었던 `답변 흐름` / `고칠 점으로 나온 유형` 소제목도 함께 사라졌다. 44px 터치 영역 수정은 그대로 남는다.
- 남은 상단 카드: 배지, 제목, 날짜, `답변 N문항 · 말한 시간 · 녹음 N개` 한 줄, 문항별 타임라인, `연습 통계`·`저장·분석 안내` 접힌 두 줄.
- 이 결정을 체크리스트 앞 설계 결정 6번으로 적었다. 되돌리지 않는다.
- 검증: `npm test` 152개 통과, `npm run typecheck` 통과, `npm run build` 통과. 390px·900px 실제 렌더링으로 다시 확인했고 가로 스크롤·콘솔 에러 없음.

### 2026-09-12 — S-02 막대 기준을 고정 눈금으로

- 회차에서 가장 긴 답변을 가득 찬 것으로 잡던 상대 눈금을 고정값으로 바꿨다. 상대 눈금은 같은 1분 답변이 회차마다 다른 길이로 보여 서로 견줄 수 없었다.
- 막대 전체는 **2:30**(`TIMELINE_FULL_SEC = 150`), 세로 눈금은 **1:40과 2:00**(`TIMELINE_MARKS_SEC = [100, 120]`)이다. 사용자가 적정 답변 길이로 1:40~2:00을 제시했다.
- 2:00을 넘긴 구간만 연한 색(`bg-primary/45`)으로 이어 붙여 넘긴 만큼이 보이게 했다. 처음에는 `bg-warn-ink`로 칠했는데 경고처럼 읽혀 되돌렸다. 오래 말한 것이 잘못은 아니므로 경고색을 쓰지 않는다.
- 2:30을 넘으면 막대가 가득 차고, 실제 시간은 오른쪽 숫자가 그대로 보여 준다.
- 눈금선은 채운 부분 위에 그려 항상 보인다. 막대 높이를 `h-1.5`에서 `h-2`로 올려 눈금이 읽히게 했다.
- 범례 한 줄을 목록 아래에 붙였다. 짧다고 경고하지 않는 원칙은 그대로다.
- 공용 `ProgressBar` 대신 이 화면 전용 `AnswerTimeBar`를 썼다. `ProgressBar`는 눈금과 초과 구간을 그릴 수 없고, 배치 진행률 등 다른 용도로 계속 쓰인다.
- 검증: `npm test` 152개 통과, `npm run typecheck` 통과, `npm run build` 통과. 0:45 / 2:12 / 2:48 세 경우를 390px·900px 실제 렌더링으로 확인했고 가로 스크롤·콘솔 에러 없음.

### 2026-09-12 — S-02 막대를 고정 눈금과 세 가지 색으로

- 회차에서 가장 긴 답변을 가득 찬 것으로 잡던 상대 눈금을 고정값으로 바꿨다. 상대 눈금은 같은 1분 답변이 회차마다 다른 길이로 보여 서로 견줄 수 없었다.
- 막대 전체는 **2:30**(`TIMELINE_FULL_SEC = 150`), 적정 구간은 사용자가 넉넉하게 잡은 **1:20 ~ 2:00**(`TIMELINE_RANGE_SEC = [80, 120]`)이며 세로 눈금으로 그린다.
- 막대 색을 세 가지로 나눴다. 적정 구간 안이면 `bg-success`(초록), 그보다 짧으면 `bg-primary/40`(연한 인디고), 길면 `bg-primary-ink`(진한 인디고)다.
- 넘긴 쪽에 경고색을 쓰지 않았다. 팔레트의 `warn-ink`는 밝은 테마에서 갈색이라 경고처럼 읽히고, 길게 말한 것이 잘못도 아니다. 짧은 쪽에도 경고 문구를 붙이지 않는다.
- 2:30을 넘으면 막대가 가득 차고 실제 시간은 오른쪽 숫자가 그대로 보여 준다.
- 눈금선은 채운 부분 위에 그려 항상 보인다. 막대 높이를 `h-1.5`에서 `h-2`로 올려 눈금이 읽히게 했다.
- 세 상태를 설명하는 범례 한 줄을 목록 아래에 붙였다.
- 공용 `ProgressBar` 대신 이 화면 전용 `AnswerTimeBar`를 썼다. `ProgressBar`는 눈금과 상태 색을 그릴 수 없고 배치 진행률 등 다른 용도로 계속 쓰인다.
- 검증: `npm test` 152개 통과, `npm run typecheck` 통과, `npm run build` 통과. 0:45(짧음) / 1:35(적정) / 2:48(김) 세 경우를 390px·900px 실제 렌더링으로 확인했고 접힌 내용·저장된 피드백 열람·키보드 초점도 다시 확인했다. 가로 스크롤·콘솔 에러 없음.

### 2026-09-12 — S-02 막대를 고정 눈금과 목표 구간 음영으로

- 회차에서 가장 긴 답변을 가득 찬 것으로 잡던 상대 눈금을 고정값으로 바꿨다. 상대 눈금은 같은 1분 답변이 회차마다 다른 길이로 보여 서로 견줄 수 없었다.
- 막대 전체는 **2:30**(`TIMELINE_FULL_SEC = 150`), 적정 구간은 넉넉하게 잡은 **1:20 ~ 2:00**(`TIMELINE_RANGE_SEC = [80, 120]`)이다.
- **길이를 색으로 판정하지 않는다.** 막대는 언제나 `bg-primary` 한 가지고, 적정 구간만 트랙에 `--time-zone` 음영으로 표시한다.
- 여기까지 오는 데 세 번 틀렸다. (1) 짧음 연한 인디고 / 적정 초록 / 김 진한 인디고 — 대비가 약했다. (2) 짧음 노랑 / 적정 인디고 / 김 주황 — 8px 막대에 원색 셋이라 경고 배너처럼 보였다. (3) 시안 네 개를 그려 사용자가 A(단색 + 목표 구간 음영)를 골랐다.
- 시안의 A는 음영이 트랙색과 비슷해 보이지 않았고 막대가 길면 음영이 덮였다. 그래서 `--time-zone`을 트랙보다 확실히 진하게 잡고(`#c7d2fe` / 다크 `#2e2a52`), 구간 양 끝에 눈금선을 막대 **위에** 그려 덮여도 목표 위치가 보이게 했다.
- 2:30을 넘으면 막대가 가득 차고 실제 시간은 오른쪽 숫자가 그대로 보여 준다.
- 막대 높이를 `h-1.5`에서 `h-2`로 올려 음영과 눈금이 읽히게 했다. 공용 `ProgressBar`는 음영·눈금을 그릴 수 없어 이 화면 전용 `AnswerTimeBar`를 따로 뒀고, `ProgressBar`는 배치 진행률에서 계속 쓴다.
- 검증: `npm test` 152개 통과, `npm run typecheck` 통과, `npm run build` 통과. 0:45 / 1:35 / 2:48 세 경우를 밝은 테마와 다크 테마에서 실제 렌더링으로 확인했고, 접힌 내용·저장된 피드백 열람·키보드 초점도 다시 확인했다. 가로 스크롤·콘솔 에러 없음.
